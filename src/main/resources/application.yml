spring:
  datasource:
    url: jdbc:mariadb://localhost:3307/springboot
    driver-class-name: org.mariadb.jdbc.Driver
    username: book_ex
    password: book_ex
  jpa:
    open-in-view: false
    generate-ddl: true
    show-sql: true
    hibernate:
      ddl-auto: update