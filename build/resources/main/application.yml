spring:
  datasource:
    url: jdbc:mariadb://localhost:3306/mp1
    driver-class-name: org.mariadb.jdbc.Driver
    username: mp
    password: mp
  jpa:
    open-in-view: false
    generate-ddl: true
    show-sql: true
    hibernate:
      ddl-auto: update